# Microservice Deployment on VirtualBox

This repository contains the source code for a microservice architecture assignment.

## Architecture
- **Frontend (VM2):** Client node that requests data.
- **Backend (VM3):** Flask API returning JSON status.
- **Network:** Private Internal Network (Air-gapped).

## Usage
Run the application:
`python3 app.py`
